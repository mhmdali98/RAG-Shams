# 🌞 بوت شركة الشمس تيليكوم - RAG Chatbot

بوت ذكي يستخدم تقنية RAG (Retrieval-Augmented Generation) للإجابة على أسئلة العملاء حول شركة الشمس تيليكوم بناءً على المعلومات المحفوظة في قاعدة بيانات متجهة.

## 📋 المحتويات

- [المميزات](#المميزات)
- [التقنيات المستخدمة](#التقنيات-المستخدمة)
- [المتطلبات](#المتطلبات)
- [التثبيت](#التثبيت)
- [الاستخدام](#الاستخدام)
- [البنية](#البنية)
- [الواجهات](#الواجهات)
- [التطوير](#التطوير)

## ✨ المميزات

- ✅ إجابة باللغة العربية الفصحى
- ✅ الإجابة بناءً على المعلومات المحفوظة فقط (منع الاختلاق)
- ✅ واجهة دردشة تفاعلية باستخدام Gradio
- ✅ واجهة برمجية (API) باستخدام FastAPI
- ✅ استرجاع ذكي للمعلومات باستخدام ChromaDB
- ✅ استخدام نموذج Mistral المحلي عبر Ollama

## 🛠 التقنيات المستخدمة

- **LangChain**: إطار عمل لبناء تطبيقات RAG
- **ChromaDB**: قاعدة بيانات متجهة لتخزين واسترجاع المعلومات
- **Ollama**: تشغيل نماذج اللغة المحلية
- **Mistral**: نموذج اللغة المستخدم
- **HuggingFace Embeddings**: نماذج التضمين للنصوص
- **Gradio**: واجهة المستخدم التفاعلية
- **FastAPI**: واجهة برمجية (API)

## 📦 المتطلبات

- Python 3.8+
- Ollama مثبت ومشغل
- نموذج Mistral مثبت في Ollama

### تثبيت Ollama ونموذج Mistral

```bash
# تثبيت Ollama (macOS)
brew install ollama

# أو قم بتحميله من الموقع الرسمي: https://ollama.ai

# تثبيت نموذج Mistral
ollama pull mistral
```

## 🚀 التثبيت

1. **استنساخ المشروع**:
```bash
git clone <repository-url>
cd RAG-Shams
```

2. **إنشاء بيئة افتراضية**:
```bash
python -m venv env
source env/bin/activate  # على Linux/Mac
# أو
env\Scripts\activate  # على Windows
```

3. **تثبيت المتطلبات**:
```bash
pip install -r requirements.txt
```

4. **التحقق من أن Ollama يعمل**:
```bash
ollama serve
```

## 💻 الاستخدام

### الواجهة التفاعلية (Gradio)

```bash
python app.py
```

ثم افتح المتصفح على: `http://localhost:7860`

### الواجهة البرمجية (FastAPI)

```bash
uvicorn api:app --reload --host 0.0.0.0 --port 8000
```

ثم افتح: `http://localhost:8000/docs` للاطلاع على التوثيق التفاعلي

#### أمثلة على استخدام الـ API

```bash
# طرح سؤال
curl -X POST "http://localhost:8000/ask" \
  -H "Content-Type: application/json" \
  -d '{"question": "من نحن؟"}'

# فحص حالة الخدمة
curl http://localhost:8000/health
```

#### مثال باستخدام Python

```python
import requests

response = requests.post(
    "http://localhost:8000/ask",
    json={"question": "ما هي خدماتكم؟"}
)

print(response.json()["answer"])
```

## 📁 البنية

```
RAG-Shams/
│
├── app.py              # واجهة Gradio التفاعلية
├── api.py              # واجهة FastAPI البرمجية
├── rag_engine.py       # محرك RAG الأساسي
├── shams-info.txt      # البيانات النصية للمعلومات
├── requirements.txt    # المتطلبات
├── README.md           # هذا الملف
├── .gitignore          # ملف Git ignore
│
└── chroma_db/          # قاعدة البيانات المتجهة (يتم إنشاؤها تلقائيًا)
```

## 🎯 الواجهات

### 1. واجهة Gradio (`app.py`)

واجهة تفاعلية بسيطة للمحادثة المباشرة:
- محادثة مباشرة مع البوت
- واجهة سهلة الاستخدام
- دعم اللغة العربية

### 2. واجهة FastAPI (`api.py`)

واجهة برمجية كاملة المميزات:
- `POST /ask`: طرح سؤال والحصول على إجابة
- `GET /health`: فحص حالة الخدمة
- `GET /`: معلومات عن الـ API
- `GET /docs`: توثيق تفاعلي (Swagger UI)

## 🔧 التطوير

### إضافة معلومات جديدة

1. أضف المعلومات إلى `shams-info.txt`
2. أعد بناء قاعدة البيانات المتجهة (يمكن إنشاء سكريبت لذلك)
3. أعد تشغيل التطبيق

### تحسينات مستقبلية

- [ ] إضافة دعم لعدة ملفات نصية
- [ ] تحسين استراتيجية الاسترجاع
- [ ] إضافة سجل للمحادثات
- [ ] دعم الصور والملفات المرفقة
- [ ] تحسين قالب التعليمات
- [ ] إضافة نظام تقييم للإجابات

## 📝 الملاحظات

- البوت يجيب فقط بناءً على المعلومات الموجودة في `shams-info.txt`
- إذا لم تكن المعلومات موجودة، سيقول: "عذرًا، لا أملك معلومات كافية حول هذا الموضوع."
- تأكد من أن Ollama يعمل قبل تشغيل التطبيق

## 🐛 استكشاف الأخطاء

### خطأ: "Connection refused" مع Ollama
```bash
# تأكد من أن Ollama يعمل
ollama serve

# في نافذة طرفية منفصلة
ollama list  # للتحقق من النماذج المثبتة
```

### خطأ: قاعدة البيانات المتجهة غير موجودة
- تأكد من وجود مجلد `chroma_db` مع البيانات
- إذا لم يكن موجودًا، ستحتاج إلى إنشاء قاعدة البيانات من `shams-info.txt`

## 📄 الترخيص

هذا المشروع خاص بشركة الشمس تيليكوم.

## 👥 المساهمون

- فريق التطوير - شركة الشمس تيليكوم

## 📞 الدعم

للدعم الفني أو الاستفسارات، يرجى التواصل مع فريق التطوير.

---

**صنع بـ ❤️ لشركة الشمس تيليكوم**

