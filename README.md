# 🌞 بوت شركة الشمس تيليكوم - RAG Chatbot

بوت ذكي يستخدم تقنية RAG (Retrieval-Augmented Generation) للإجابة على أسئلة العملاء حول شركة الشمس تيليكوم بناءً على المعلومات المحفوظة في قاعدة بيانات متجهة.

## 📁 هيكل المشروع

```
RAG-Shams/
├── src/                    # الكود المصدري
│   ├── __init__.py
│   ├── core/              # المكونات الأساسية
│   │   ├── __init__.py
│   │   ├── rag_engine.py  # محرك RAG
│   │   └── suggestions.py # نظام الاقتراحات
│   ├── api/               # واجهة API
│   │   ├── __init__.py
│   │   ├── main.py        # FastAPI app
│   │   └── models.py      # Pydantic models
│   └── ui/                # واجهة المستخدم
│       ├── __init__.py
│       └── app.py         # Gradio app
├── data/                   # البيانات
│   └── shams-info.txt     # معلومات الشركة
├── scripts/                # السكريبتات
│   ├── rebuild_vectorstore.py
│   └── start.sh
├── config/                 # الإعدادات
│   ├── __init__.py
│   └── settings.py         # إعدادات المشروع
├── storage/                # التخزين
│   └── chroma_db/         # قاعدة البيانات المتجهة
├── tests/                  # الاختبارات
├── .gitignore
├── requirements.txt
└── README.md
```

## ✨ المميزات

- ✅ إجابة باللغة العربية الفصحى فقط
- ✅ الإجابة بناءً على المعلومات المحفوظة فقط (منع الاختلاق)
- ✅ واجهة دردشة تفاعلية باستخدام Gradio
- ✅ واجهة برمجية (API) باستخدام FastAPI
- ✅ استرجاع ذكي للمعلومات باستخدام ChromaDB
- ✅ نظام اقتراحات ذكي للأسئلة
- ✅ تحقق من صحة الإجابات
- ✅ هيكل احترافي ومنظم

## 🛠 التقنيات المستخدمة

- **LangChain**: إطار عمل لبناء تطبيقات RAG
- **ChromaDB**: قاعدة بيانات متجهة لتخزين واسترجاع المعلومات
- **Ollama**: تشغيل نماذج اللغة المحلية
- **LLaMA3**: نموذج اللغة المستخدم
- **HuggingFace Embeddings**: نماذج التضمين للنصوص
- **Gradio**: واجهة المستخدم التفاعلية
- **FastAPI**: واجهة برمجية (API)

## 📦 المتطلبات

- Python 3.8+
- Ollama مثبت ومشغل
- نموذج LLaMA3 مثبت في Ollama

### تثبيت Ollama ونموذج LLaMA3

```bash
# تثبيت Ollama (macOS)
brew install ollama

# أو قم بتحميله من الموقع الرسمي: https://ollama.ai

# تثبيت نموذج LLaMA3
ollama pull llama3
```

## 🚀 التثبيت

1. **استنساخ المشروع**:
```bash
git clone <repository-url>
cd RAG-Shams
```

2. **إنشاء بيئة افتراضية**:
```bash
python -m venv env
source env/bin/activate  # على Linux/Mac
# أو
env\Scripts\activate  # على Windows
```

3. **تثبيت المتطلبات**:
```bash
pip install -r requirements.txt
```

4. **إعداد ملف الإعدادات (اختياري)**:
```bash
cp .env.example .env
# عدّل .env حسب احتياجاتك
```

5. **بناء قاعدة البيانات المتجهة**:
```bash
python scripts/rebuild_vectorstore.py
```

## 💻 الاستخدام

### الطريقة السريعة

```bash
./scripts/start.sh
```

### الواجهة التفاعلية (Gradio)

```bash
python -m src.ui.app
```

ثم افتح المتصفح على: `http://localhost:7860`

### الواجهة البرمجية (FastAPI)

```bash
uvicorn src.api.main:app --reload --host 0.0.0.0 --port 8000
```

ثم افتح: `http://localhost:8000/docs` للاطلاع على التوثيق التفاعلي

#### أمثلة على استخدام الـ API

```bash
# طرح سؤال
curl -X POST "http://localhost:8000/ask" \
  -H "Content-Type: application/json" \
  -d '{"question": "من نحن؟"}'

# الحصول على اقتراحات
curl -X POST "http://localhost:8000/suggestions" \
  -H "Content-Type: application/json" \
  -d '{"question": "باقات", "num_suggestions": 5}'

# فحص حالة الخدمة
curl http://localhost:8000/health
```

## 🔧 الإعدادات

يمكنك تعديل الإعدادات في `config/settings.py` أو عبر متغيرات البيئة في ملف `.env`:

- `LLM_MODEL`: نموذج اللغة (افتراضي: llama3)
- `LLM_TEMPERATURE`: درجة الحرارة للنموذج (افتراضي: 0.01)
- `RETRIEVER_K`: عدد المستندات المسترجعة (افتراضي: 10)
- `API_PORT`: منفذ API (افتراضي: 8000)
- `UI_PORT`: منفذ UI (افتراضي: 7860)

## 📝 تحديث البيانات

عند تحديث `data/shams-info.txt`:

```bash
python scripts/rebuild_vectorstore.py
```

## 🎯 الواجهات

### 1. واجهة Gradio (`src/ui/app.py`)

واجهة تفاعلية بسيطة للمحادثة المباشرة:
- محادثة مباشرة مع البوت
- واجهة سهلة الاستخدام
- دعم اللغة العربية
- اقتراحات ذكية للأسئلة

### 2. واجهة FastAPI (`src/api/main.py`)

واجهة برمجية كاملة المميزات:
- `POST /ask`: طرح سؤال والحصول على إجابة
- `POST /suggestions`: الحصول على اقتراحات للأسئلة
- `GET /health`: فحص حالة الخدمة
- `GET /`: معلومات عن الـ API
- `GET /docs`: توثيق تفاعلي (Swagger UI)

## 🔍 المكونات الرئيسية

### `src/core/rag_engine.py`
محرك RAG الأساسي الذي:
- يحمل قاعدة البيانات المتجهة
- يسترجع المعلومات ذات الصلة
- يولد الإجابات باستخدام LLM

### `src/core/suggestions.py`
نظام توليد الاقتراحات الذي:
- يكتشف موضوع السؤال
- يولد اقتراحات متعلقة
- يوفر اقتراحات عامة

### `config/settings.py`
إعدادات المشروع المركزية

## 🐛 استكشاف الأخطاء

### خطأ: "Connection refused" مع Ollama
```bash
# تأكد من أن Ollama يعمل
ollama serve

# في نافذة طرفية منفصلة
ollama list  # للتحقق من النماذج المثبتة
```

### خطأ: قاعدة البيانات المتجهة غير موجودة
```bash
# أعد بناء قاعدة البيانات
python scripts/rebuild_vectorstore.py
```

### خطأ: ModuleNotFoundError
```bash
# تأكد من تفعيل البيئة الافتراضية
source env/bin/activate

# تأكد من تثبيت المتطلبات
pip install -r requirements.txt
```

## 📄 الترخيص

هذا المشروع خاص بشركة الشمس تيليكوم.

## 👥 المساهمون

- فريق التطوير - شركة الشمس تيليكوم

## 📞 الدعم

للدعم الفني أو الاستفسارات، يرجى التواصل مع فريق التطوير.

---

**صنع بـ ❤️ لشركة الشمس تيليكوم**
