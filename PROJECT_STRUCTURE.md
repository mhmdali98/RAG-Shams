# 📁 هيكل المشروع - Project Structure

## نظرة عامة

تم إعادة هيكلة المشروع بشكل احترافي ليكون أكثر وضوحاً وسهولة في الصيانة والتطوير.

## الهيكل الكامل

```
RAG-Shams/
│
├── src/                          # الكود المصدري الرئيسي
│   ├── __init__.py               # تعريف الحزمة
│   │
│   ├── core/                     # المكونات الأساسية
│   │   ├── __init__.py           # تصدير الوظائف الرئيسية
│   │   ├── rag_engine.py         # محرك RAG (القلب النابض)
│   │   └── suggestions.py        # نظام توليد الاقتراحات
│   │
│   ├── api/                      # واجهة برمجية (REST API)
│   │   ├── __init__.py           # تصدير app
│   │   ├── main.py               # FastAPI application
│   │   └── models.py             # Pydantic models
│   │
│   └── ui/                       # واجهة المستخدم
│       ├── __init__.py
│       └── app.py                # Gradio application
│
├── data/                         # البيانات والمعلومات
│   └── shams-info.txt            # معلومات الشركة (قاعدة المعرفة)
│
├── scripts/                      # السكريبتات المساعدة
│   ├── rebuild_vectorstore.py   # إعادة بناء قاعدة البيانات المتجهة
│   └── start.sh                  # سكريبت بدء التشغيل
│
├── config/                       # الإعدادات
│   ├── __init__.py               # تصدير Settings
│   └── settings.py               # جميع إعدادات المشروع
│
├── storage/                      # التخزين
│   └── chroma_db/                # قاعدة البيانات المتجهة (يتم إنشاؤها تلقائياً)
│
├── tests/                        # الاختبارات (للتطوير المستقبلي)
│   └── __init__.py
│
├── env/                          # البيئة الافتراضية (يتم إنشاؤها)
│
├── .gitignore                    # ملفات Git ignore
├── .env.example                   # مثال على ملف الإعدادات
├── requirements.txt               # المتطلبات
└── README.md                     # التوثيق الرئيسي
```

## شرح المجلدات

### `src/` - الكود المصدري

#### `src/core/` - المكونات الأساسية
- **rag_engine.py**: محرك RAG الرئيسي
  - تحميل قاعدة البيانات المتجهة
  - استرجاع المعلومات
  - توليد الإجابات
  - التحقق من صحة الإجابات
  
- **suggestions.py**: نظام الاقتراحات
  - اكتشاف موضوع السؤال
  - توليد اقتراحات ذكية
  - اقتراحات متعلقة

#### `src/api/` - واجهة برمجية
- **main.py**: تطبيق FastAPI
  - `/ask`: طرح سؤال
  - `/suggestions`: الحصول على اقتراحات
  - `/health`: فحص الحالة
  - `/docs`: التوثيق التفاعلي

- **models.py**: نماذج Pydantic
  - `QuestionRequest`
  - `AnswerResponse`
  - `SuggestionsRequest`
  - `SuggestionsResponse`

#### `src/ui/` - واجهة المستخدم
- **app.py**: تطبيق Gradio
  - واجهة محادثة تفاعلية
  - اقتراحات تلقائية
  - تصميم احترافي

### `data/` - البيانات
- **shams-info.txt**: ملف المعلومات الرئيسي
  - معلومات الشركة
  - الباقات والأسعار
  - الخدمات
  - الأسئلة الشائعة

### `scripts/` - السكريبتات
- **rebuild_vectorstore.py**: إعادة بناء قاعدة البيانات
  - قراءة `data/shams-info.txt`
  - تجزئة النص
  - إنشاء قاعدة بيانات متجهة

- **start.sh**: سكريبت بدء التشغيل
  - تفعيل البيئة
  - التحقق من قاعدة البيانات
  - تشغيل UI أو API

### `config/` - الإعدادات
- **settings.py**: إعدادات مركزية
  - إعدادات LLM
  - إعدادات Retriever
  - إعدادات API/UI
  - مسارات الملفات

### `storage/` - التخزين
- **chroma_db/**: قاعدة البيانات المتجهة
  - يتم إنشاؤها تلقائياً
  - تحتوي على embeddings للمعلومات

## تدفق العمل

### 1. التهيئة
```
config/settings.py → تحميل الإعدادات
src/core/rag_engine.py → تهيئة RAG engine
```

### 2. معالجة السؤال
```
User Question → src/core/rag_engine.py
              → Retriever (ChromaDB)
              → LLM (Ollama)
              → Response
```

### 3. توليد الاقتراحات
```
Question → src/core/suggestions.py
        → detect_topic()
        → get_suggestions()
        → Suggestions List
```

## المزايا

### ✅ التنظيم
- فصل واضح بين المكونات
- سهولة الصيانة
- قابلية التوسع

### ✅ الإعدادات المركزية
- جميع الإعدادات في مكان واحد
- سهولة التعديل
- دعم متغيرات البيئة

### ✅ الوضوح
- أسماء ملفات واضحة
- هيكل منطقي
- توثيق شامل

### ✅ الاحترافية
- اتباع أفضل الممارسات
- هيكل قابل للتوسع
- سهولة الاختبار

## كيفية الاستخدام

### إضافة ميزة جديدة

1. **ميزة في Core**: أضف في `src/core/`
2. **Endpoint جديد**: أضف في `src/api/main.py`
3. **UI component**: أضف في `src/ui/app.py`

### تعديل الإعدادات

1. عدّل `config/settings.py`
2. أو أنشئ ملف `.env` من `.env.example`

### تحديث البيانات

1. عدّل `data/shams-info.txt`
2. شغّل `python scripts/rebuild_vectorstore.py`

## ملاحظات

- جميع الـ imports تستخدم المسارات النسبية
- الإعدادات مركزية في `config/settings.py`
- قاعدة البيانات في `storage/chroma_db/`
- البيانات في `data/shams-info.txt`

---

**تم إعادة الهيكلة بـ ❤️ لسهولة الصيانة والتطوير**

