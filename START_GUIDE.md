# 🚀 دليل البدء السريع - تحديث البيانات وتشغيل البوت

## 📋 الخطوات المطلوبة

### الطريقة 1: استخدام السكريبت (الأسهل)

```bash
# إعادة بناء قاعدة البيانات وتحديثها
./start_fresh.sh
```

### الطريقة 2: خطوة بخطوة

#### 1️⃣ تفعيل البيئة الافتراضية
```bash
source env/bin/activate
```

#### 2️⃣ تحديث البيانات (إن أردت)
قم بتعديل ملف `shams-info.txt` وأضف أو عدّل المعلومات المطلوبة.

#### 3️⃣ إعادة بناء قاعدة البيانات المتجهة
```bash
python rebuild_vectorstore.py
```

هذا الأمر سيقوم بـ:
- ✅ حذف قاعدة البيانات القديمة
- ✅ قراءة `shams-info.txt`
- ✅ تجزئة النص بشكل ذكي
- ✅ إنشاء قاعدة بيانات متجهة جديدة
- ✅ اختبار الاسترجاع

#### 4️⃣ تشغيل البوت

**لل واجهة التفاعلية (Gradio):**
```bash
python app.py
```
ثم افتح المتصفح على: `http://localhost:7860`

**للـ API:**
```bash
uvicorn api:app --reload --host 0.0.0.0 --port 8000
```
ثم افتح: `http://localhost:8000/docs`

## ⚠️ متى تحتاج لإعادة بناء قاعدة البيانات؟

- ✅ عند تحديث `shams-info.txt` (إضافة/تعديل/حذف معلومات)
- ✅ عند تغيير إعدادات التجزئة في `rebuild_vectorstore.py`
- ✅ عند أول تشغيل للبوت
- ✅ عند حدوث مشاكل في الاسترجاع

## 🔄 تحديث البيانات فقط (بدون إعادة بناء)

إذا كنت تريد فقط **تشغيل البوت** بدون تحديث البيانات:

```bash
# تفعيل البيئة
source env/bin/activate

# تشغيل الواجهة
python app.py

# أو تشغيل API
uvicorn api:app --reload
```

## 📝 ملاحظات مهمة

1. **تأكد من أن Ollama يعمل:**
   ```bash
   ollama serve
   ```

2. **تأكد من وجود النموذج:**
   ```bash
   ollama list
   # يجب أن ترى llama3 في القائمة
   ```

3. **إذا لم يكن موجودًا:**
   ```bash
   ollama pull llama3
   ```

## 🎯 سيناريوهات الاستخدام

### السيناريو 1: أول تشغيل
```bash
./start_fresh.sh
python app.py
```

### السيناريو 2: تحديث البيانات فقط
1. عدّل `shams-info.txt`
2. `python rebuild_vectorstore.py`
3. `python app.py`

### السيناريو 3: تشغيل فقط (بدون تحديث)
```bash
source env/bin/activate
python app.py
```

## ✅ التحقق من نجاح العملية

بعد إعادة بناء قاعدة البيانات، يجب أن ترى:
- ✅ رسالة "تم حفظ قاعدة البيانات المتجهة"
- ✅ نتائج اختبار الاسترجاع
- ✅ رسالة "اكتملت إعادة بناء قاعدة البيانات المتجهة بنجاح"

## 🐛 استكشاف الأخطاء

### خطأ: "Connection refused" مع Ollama
```bash
# تأكد من أن Ollama يعمل
ollama serve
```

### خطأ: قاعدة البيانات غير موجودة
```bash
# أعد بناء قاعدة البيانات
python rebuild_vectorstore.py
```

### خطأ: النموذج غير موجود
```bash
# ثبّت النموذج
ollama pull llama3
```

---

**جاهز للبدء! 🚀**

