# 🧪 تشغيل اختبار دقة النموذج

## خطوات سريعة

### 1. إعادة بناء قاعدة البيانات (مهم جداً!)

```bash
# تأكد من تفعيل البيئة الافتراضية أولاً
source env/bin/activate

# إعادة بناء قاعدة البيانات
python scripts/rebuild_vectorstore.py
```

### 2. تشغيل الاختبار

```bash
python tests/test_rag_accuracy.py
```

## 📊 ما ستحصل عليه

- ✅ عدد الإجابات الصحيحة من أصل 30
- 📈 الدقة الإجمالية (نسبة مئوية)
- 📋 الدقة حسب كل فئة (باقات، أسعار، تغطية، إلخ)
- ❌ قائمة الأسئلة التي فشلت
- ✅ قائمة الأسئلة الصحيحة
- 💾 ملف JSON يحتوي على النتائج التفصيلية

## ⚠️ ملاحظات مهمة

1. **يجب إعادة بناء قاعدة البيانات** بعد أي تعديلات على:
   - `data/shams-info.txt`
   - `scripts/rebuild_vectorstore.py`
   - أي تحسينات على metadata

2. **الاختبار يستغرق وقتاً** (حوالي 5-10 دقائق) لأن كل سؤال يحتاج معالجة

3. **معايير النجاح**:
   - يجب أن تحتوي الإجابة على **70% على الأقل** من الكلمات المفتاحية
   - يجب **ألا تحتوي** على كلمات ممنوعة (مثل مناطق غير صحيحة)

## 📁 ملفات النتائج

النتائج تُحفظ تلقائياً في:
```
tests/test_results_YYYYMMDD_HHMMSS.json
```

يمكنك فتح الملف لمشاهدة النتائج التفصيلية.

